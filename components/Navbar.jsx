const Navbar = () => {
  return (
    <div>
      Navbar
      <div>
        <a href="/auth/google">Login</a>
        <a href="/api/logout">Logout</a>
      </div>
    </div>
  );
};

export default Navbar;
